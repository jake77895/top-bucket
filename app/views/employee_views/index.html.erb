<div class="container mb-4 mt-4">
  <div class="card shadow-sm position-relative">
    <!-- Back Button -->
    <%= link_to root_path, class: 'btn btn-light rounded-circle shadow-sm d-flex align-items-center justify-content-center position-absolute', style: 'width: 40px; height: 40px; top: 10px; left: 10px;' do %>
      <span class="fs-6">&larr;</span>
    <% end %>

    <!-- Title and Description -->
    <div class="card-body text-center bg-light">
      <h1 class="fw-bold text-uppercase" style="letter-spacing: 1px; margin-bottom: 10px; margin-top: 10px;">Employee Insights</h1>
      <p class="text-muted" style="font-size: 14px;">Real feedback on interviews and networking from those who’ve been there</p>
    </div>
  </div>
</div>

<!-- Employee Views Table -->
<div class="container">
  <div class="card shadow-sm border-0">
    <table class="table mb-0">
      <thead class="table-dark text-white">
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th>Number of Employees</th>
        </tr>
      </thead>
      <tbody>
        <% @employee_views.each do |employee_view| %>
          <tr>
            <td><%= link_to employee_view.name, employee_view_path(employee_view), style: "text-decoration: none; color: black;" %></td>
            <td><%= employee_view.description %></td>
            <td><%= employee_view.employee_count %></td>

          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>
</div>
