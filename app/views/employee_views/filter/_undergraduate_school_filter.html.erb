<div class="modal fade" id="undergraduateSchoolFilterModal" tabindex="-1" aria-labelledby="undergraduateSchoolFilterLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="undergraduateSchoolFilterLabel">Filter by Undergraduate School</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%= search_form_for @q, url: employee_view_path(@employee_view), method: :get, html: { class: 'filter-form' } do |f| %>
          <div class="mb-3">
            <%= f.label :undergraduate_school_id_eq, "Select Undergraduate School", class: 'form-label' %>
            <%= f.collection_select :undergraduate_school_id_eq, School.joins("INNER JOIN employees ON employees.undergraduate_school_id = schools.id").where(employees: { id: @employee_view.employee_ids }).distinct, :id, :name, { prompt: 'All Undergraduate Schools' }, { class: 'form-select' } %>
          </div>
          <div class="text-end">
            <%= f.submit 'Apply Filter', class: 'btn btn-primary' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
