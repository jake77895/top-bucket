<div class="modal fade" id="locationFilterModal" tabindex="-1" aria-labelledby="locationFilterLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="locationFilterLabel">Filter by Location</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <%= search_form_for @q, url: employee_view_path(@employee_view), method: :get, html: { class: 'filter-form' } do |f| %>
          <div class="mb-3">
            <%= f.label :location_id_eq, "Select Location", class: 'form-label' %>
            <%= f.collection_select :location_id_eq, Location.joins(:employees).where(employees: { id: @employee_view.employee_ids }).distinct, :id, :name, { prompt: 'All Locations' }, { class: 'form-select' } %>
          </div>
          <div class="text-end">
            <%= f.submit 'Apply Filter', class: 'btn btn-primary' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
