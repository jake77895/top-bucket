<div class="container mt-4">
  <div class="card">
    <div class="card-header">
      <h2>New Exit Opportunity Detail</h2>
      <% if @mapping %>
        <p class="text-muted mb-0">
          For: <%= @mapping.source_category %> → <%= @mapping.target_industry %> 
          (<%= @mapping.target_node_name %>)
        </p>
      <% end %>
    </div>
    <div class="card-body">
      <%= render 'form' %>
    </div>
  </div>
</div> 
